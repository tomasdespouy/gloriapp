{
  "name": "gloria-voice-relay",
  "version": "0.1.0",
  "description": "Rele de voz del piloto Fernanda LatAm (docs/specs/paciente-voz-latam). Dueno de la conexion real a OpenAI Realtime; el navegador le habla por WebSocket a este servicio, nunca directo a OpenAI.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
